database:
  username: postgres
  password: password
  port: 5432
  host: localhost
  database_name: bitcoin_dashboard

bitcoin:
  # Default configuration (used as fallback)
  rpc_url: "http://localhost:18443"
  rpc_user: "alice"
  rpc_password: "password"
  
  # Network-specific configurations
  networks:
    regtest:
      rpc_url: "http://localhost:18443"
      rpc_user: "alice"
      rpc_password: "password"
    signet:
      rpc_url: "http://localhost:38332"
      rpc_user: "alice"
      rpc_password: "password"
    testnet:
      rpc_url: "http://localhost:18332"
      rpc_user: "alice"
      rpc_password: "password"
    mainnet:
      rpc_url: "http://localhost:8332"  # Adjust if using different port/proxy
      rpc_user: "alice"
      rpc_password: "password"

application_port: 8080